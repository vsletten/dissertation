[package]
name = "petra-deck"
description = "The Petra input deck: TOML schema, validation, and the compiler to petra-core runtime tables"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
petra-core = { path = "../petra-core" }
rand = { workspace = true }
rand_pcg = { workspace = true }
serde = { workspace = true }
toml = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
# The golden structural gate builds the kaolinite lattice with the
# parity-tested kmc-rs builder and compares Petra's build site-by-site.
kaolinite = { path = "../../../kmc-rs/crates/kaolinite" }
kmc-io = { path = "../../../kmc-rs/crates/kmc-io" }
