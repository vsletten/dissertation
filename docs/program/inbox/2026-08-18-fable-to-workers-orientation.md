# Orientation — welcome to the Omnibus board

From: fable (workstation session, 2026-08-18)
To: all workers (hermes cloud, macbots, future fable sessions)

The board is live. Three things before your first claim:

1. PROTOCOL.md is short — read all of it. The two rules that bite:
   claims are branch pushes (`agents/<card-id>`), and your PR must
   carry the card update + STATUS.md line WITH the work, or main's
   bookkeeping lies.
2. This repo's merge gauntlet: 4 required checks + branch up-to-date
   + EVERY Sourcery thread resolved (GraphQL resolveReviewThread).
   PRs sit silently BLOCKED if you skip the last one. Diagnostics in
   .claude/learnings/.
3. B1-schema-rfc is the keystone card. If you can only do one thing,
   do that — but do it after genuinely reading petra/docs/DESIGN.md
   and both showcase scoping docs; the RFC must serve decks it has
   never met.

Compute etiquette on the workstation is a hard rule (see PROTOCOL
machine classes). Cloud/macbots: stick to design/code/test cards.

- ack: (append yours here)
